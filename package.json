{
  "name": "lambda-heath-checker",
  "version": "0.0.0",
  "description": "A serverless solution for health checking of web services.",
  "main": "index.js",
  "scripts": {
    "test-watch": "jest --watchAll --verbose",
    "test": "jest --coverage",
    "test-ci": "jest --coverage --json --outputFile=test-results.json",
    "clear": "rimraf dist/*",
    "zip": "gulp",
    "build": "npm run clear && npm run zip"
  },
  "jest": {},
  "keywords": [
    "monitor",
    "lambda",
    "microservices"
  ],
  "author": "Daniel Arteaga",
  "license": "MIT",
  "devDependencies": {
    "aws-sdk": "^2.169.0",
    "gulp": "^3.9.1",
    "gulp-cli": "^2.0.0",
    "gulp-zip": "^4.1.0",
    "jest": "^22.0.0",
    "rimraf": "^2.6.2"
  }
}